# Локальная разработка MCP сервера для Claude Code

Краткий гайд для разработки и тестирования MCP сервера в **Claude Code** (CLI/терминал, не Claude Desktop!).

## Быстрый старт

### 1. Установите зависимости
```bash
npm install
```

### 2. Протестируйте сервер локально
Запустите сервер напрямую чтобы проверить что он работает:
```bash
node server/index.js
```

Вы должны увидеть: `Hello World MCP server running...`

Сервер ждет MCP протокола на stdin. Нажмите Ctrl+C для выхода.

### 3. Добавьте MCP сервер в Claude Code

**Вариант A (Рекомендуемый): Через CLI команду**

```bash
claude mcp add --transport stdio hello-world-node -- node $(pwd)/server/index.js
```

Или с абсолютным путем:
```bash
claude mcp add --transport stdio hello-world-node -- node /Users/username/path/to/mcp-example/server/index.js
```

**Вариант B: Создайте .mcp.json в корне проекта**

Создайте файл `.mcp.json`:
```json
{
  "mcpServers": {
    "hello-world-node": {
      "type": "stdio",
      "command": "node",
      "args": [
        "/Users/username/path/to/mcp-example/server/index.js"
      ]
    }
  }
}
```

⚠️ **ВАЖНО:**
- Используйте **абсолютный путь** к вашему server/index.js
- Замените путь на свой реальный путь к проекту!
- Используйте `"type": "stdio"` (в Claude Code), не просто `"command"` как в Desktop

### 4. Проверьте подключение

**Через CLI:**
```bash
claude mcp list
```

Вы должны увидеть:
```
hello-world-node: node /path/to/server/index.js - ✓ Connected
```

**В Claude Code (в этом чате):**

Просто начните использовать! Claude Code автоматически получит доступ к инструменту `get_current_time`.

### 5. Используйте MCP инструменты

После подключения инструменты вашего сервера автоматически доступны Claude. Просто попросите:

```
Какое сейчас время?
```

Claude автоматически использует инструмент `get_current_time` из вашего локального сервера!

## Структура конфигурации

Claude Code хранит MCP серверы в `~/.claude.json` под ключом проекта:

```json
{
  "projects": {
    "/Users/username/path/to/mcp-example": {
      "mcpServers": {
        "hello-world-node": {
          "type": "stdio",
          "command": "node",
          "args": ["/absolute/path/to/server/index.js"],
          "env": {}
        }
      }
    }
  }
}
```

## Быстрая отладка

### Проверить что сервер запускается
```bash
node server/index.js
# Должно вывести: Hello World MCP server running...
```

### Проверить список MCP серверов
```bash
claude mcp list
```

Должно показать статус Connected:
```
context7: npx -y @upstash/context7-mcp - ✓ Connected
hello-world-node: node /path/to/server/index.js - ✓ Connected
```

### Посмотреть детали конкретного сервера
```bash
claude mcp get hello-world-node
```

### Удалить сервер
```bash
claude mcp remove hello-world-node
```

### Типичные проблемы

**Сервер не появляется в списке:**
- Правильный ли абсолютный путь в команде?
- Установлены ли зависимости (`npm install`)?
- Используете ли вы Claude Code (а не Claude Desktop)?

**Ошибка при запуске сервера:**
- Проверьте что Node.js >= 16.0.0: `node --version`
- Проверьте что зависимости установлены: `npm list`
- Запустите `claude mcp list` для проверки статуса

**Сервер показывает ❌ или ⚠️:**
- Проверьте stderr вывод сервера
- Убедитесь что путь к index.js правильный
- Попробуйте запустить `node server/index.js` вручную

**Инструменты не работают:**
- Сервер должен быть ✓ Connected в `claude mcp list`
- Перезапустите Claude Code если только что добавили сервер
- Проверьте что MCP SDK установлен: `npm list @modelcontextprotocol/sdk`

## Цикл разработки

### ⚠️ Важно: Hot Reload Ограничения

**Текущее состояние:** Claude Code не поддерживает автоматический hot reload MCP серверов. После изменения кода сервера требуется ручная перезагрузка.

**Почему так:** MCP серверы работают как долгоживущие stdio процессы. Изменения в коде не применяются автоматически - нужно перезапустить процесс.

---

### Способ 1: Встроенная команда `/mcp reconnect` (Самый быстрый) ⚡

**Рекомендуется для активной разработки!**

После изменения кода в `server/index.js`:

1. **В чате Claude Code введите:**
   ```
   /mcp reconnect hello-world-node
   ```

2. **Или используйте интерактивное меню:**
   ```
   /mcp
   ```
   Затем выберите ваш сервер и нажмите "Reconnect"

**Преимущества:**
- ✅ Не нужно выходить из чата
- ✅ Сохраняется контекст беседы
- ✅ Быстрее всех других методов

**Известные особенности:**
- Иногда требуется 2 попытки reconnect
- Команда доступна с версии Claude Code v1.0.64+

---

### Способ 2: Скрипт перезагрузки (Надежный)

Если `/mcp reconnect` не работает, используйте скрипт:

```bash
npm run dev:reload
```

Или напрямую:
```bash
./dev-reload.sh
```

Этот скрипт автоматически:
- ✅ Удалит старый сервер из конфигурации
- ✅ Добавит обновленную версию
- ✅ Проверит статус подключения

**Минус:** Нужно выполнить в отдельном терминале

---

### Способ 3: Перезапуск сессии Claude Code (Полный reset)

```bash
# Выйдите из текущей сессии
exit  # или Ctrl+D

# Запустите Claude Code снова
claude
```

При новой сессии все MCP серверы перезапускаются автоматически.

**Минусы:**
- ❌ Теряется контекст беседы
- ❌ Самый медленный способ

---

### Способ 4: Ручная перезагрузка через CLI

```bash
# Из другого терминала
npm run mcp:remove && npm run mcp:add

# Или команды напрямую
claude mcp remove hello-world-node
claude mcp add --transport stdio hello-world-node -- node $(pwd)/server/index.js
```

---

### 💡 Рекомендуемый процесс разработки

1. **Измените код** в `server/index.js`
2. **Сохраните файл** (Ctrl+S)
3. **В чате Claude Code введите:** `/mcp reconnect hello-world-node`
4. **Протестируйте** новый функционал сразу в том же чате!

### 🔍 Альтернатива: Локальное тестирование

Перед использованием в Claude Code можно протестировать локально:

```bash
# Запустите сервер вручную
npm start

# В другом терминале протестируйте с помощью reloaderoo
reloaderoo inspect list-tools -- node server/index.js
reloaderoo inspect call-tool get_current_time -- node server/index.js
```

---

### Полезные npm команды

```bash
npm start            # Запустить сервер локально (для тестирования)
npm run dev:reload   # Перезагрузить MCP сервер в Claude Code (полная перезагрузка)
npm run mcp:list     # Показать все MCP серверы
npm run mcp:status   # Показать статус hello-world-node
npm run mcp:add      # Добавить сервер
npm run mcp:remove   # Удалить сервер
```

---

### 🔬 Экспериментальные решения

**Примечание:** Следующие инструменты пока не работают надежно с Claude Code, но могут работать в будущем:

- **mcpmon** - прозрачный прокси с auto-reload (пока не в npm)
- **reloaderoo** - прокси + CLI инструменты (работает локально, но не с Claude Code stdio)
- **nodemon** - автоперезапуск процесса (теряется соединение с Claude Code)

Следите за обновлениями в [официальной документации MCP](https://modelcontextprotocol.io/)

## Разница между Claude Code и Claude Desktop

| Аспект | Claude Code | Claude Desktop |
|--------|-------------|----------------|
| **Интерфейс** | CLI/терминал | GUI приложение |
| **Конфигурация** | `~/.claude.json` | `~/Library/Application Support/Claude/claude_desktop_config.json` |
| **CLI команды** | ✅ `claude mcp add` | ❌ Нет CLI |
| **Project scope** | ✅ Есть | ❌ Нет |
| **`.mcp.json`** | ✅ Поддерживается | ❌ Не поддерживается |
| **GUI настройка** | ❌ Нет | ✅ Settings > Extensions |
| **Формат конфига** | `"type": "stdio"` | Без `type` |

## Команды управления MCP

```bash
# Добавить stdio сервер (локальный процесс)
claude mcp add --transport stdio <name> -- <command> <args...>

# Добавить HTTP сервер (удаленный)
claude mcp add --transport http <name> <url>

# Список всех серверов с проверкой здоровья
claude mcp list

# Детали конкретного сервера
claude mcp get <name>

# Удалить сервер
claude mcp remove <name>

# Импортировать из Claude Desktop конфига
claude mcp add-from-claude-desktop
```

## Примеры добавления разных типов серверов

### Локальный Node.js сервер (ваш случай)
```bash
claude mcp add --transport stdio hello-world-node -- node /path/to/server/index.js
```

### NPM пакет (например context7)
```bash
claude mcp add --transport stdio context7 -- npx -y @upstash/context7-mcp
```

### Python сервер
```bash
claude mcp add --transport stdio my-python-server -- python /path/to/server.py
```

### С переменными окружения
```bash
claude mcp add --transport stdio my-server --env API_KEY=your-key -- node /path/to/server.js
```

## Что дальше?

### Добавьте новые инструменты

Отредактируйте `server/index.js` и добавьте новые инструменты:

1. **Зарегистрируйте инструмент** в `ListToolsRequestSchema` handler:
```javascript
server.setRequestHandler(ListToolsRequestSchema, async () => {
  return {
    tools: [
      {
        name: "get_current_time",
        description: "Get the current computer time",
        inputSchema: { type: "object", properties: {} }
      },
      {
        name: "your_new_tool", // <-- Новый инструмент
        description: "Description of what it does",
        inputSchema: {
          type: "object",
          properties: {
            param1: { type: "string", description: "..." }
          }
        }
      }
    ]
  };
});
```

2. **Реализуйте логику** в `CallToolRequestSchema` handler:
```javascript
server.setRequestHandler(CallToolRequestSchema, async (request) => {
  if (request.params.name === "your_new_tool") {
    // Ваша логика здесь
    return {
      content: [{ type: "text", text: "Result..." }]
    };
  }
  // ... остальные инструменты
});
```

3. **Обновите manifest.json** (для будущей публикации):
```json
{
  "tools": [
    {
      "name": "get_current_time",
      "description": "Get the current computer time"
    },
    {
      "name": "your_new_tool",
      "description": "Description"
    }
  ]
}
```

4. **Протестируйте** в Claude Code - просто используйте новый инструмент!

### Создайте .mcpb пакет для Claude Desktop

Когда будете готовы распространять через Claude Desktop Extensions:

1. Заполните `manifest.json` всеми деталями
2. Добавьте `icon.png` (512x512 пикселей)
3. Упакуйте используя официальные инструменты
4. Распространяйте как Desktop Extension

### Опубликуйте в npm

Для широкого распространения:

```bash
npm publish
```

Тогда другие смогут использовать через npx:
```bash
claude mcp add --transport stdio your-server -- npx -y your-package-name
```

## Полезные ссылки

- [Официальная документация MCP](https://modelcontextprotocol.io/)
- [Claude Code документация](https://docs.claude.com/)
- [MCP SDK для Node.js](https://github.com/modelcontextprotocol/typescript-sdk)
- [Примеры MCP серверов](https://github.com/modelcontextprotocol/servers)
- [Claude Code CLI reference](https://docs.claude.com/en/docs/claude-code/cli-reference)
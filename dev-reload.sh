#!/bin/bash

# Скрипт для быстрой перезагрузки MCP сервера во время разработки

SERVER_NAME="hello-world-node"
SERVER_PATH="$(cd "$(dirname "$0")" && pwd)/server/index.js"

echo "🔄 Перезагрузка MCP сервера: $SERVER_NAME"
echo "📂 Путь: $SERVER_PATH"
echo ""

# Удаляем старый сервер
echo "⏳ Удаление старого сервера..."
claude mcp remove "$SERVER_NAME" 2>/dev/null || true

# Добавляем сервер заново
echo "➕ Добавление обновленного сервера..."
claude mcp add --transport stdio "$SERVER_NAME" -- node "$SERVER_PATH"

# Проверяем статус
echo ""
echo "✅ Проверка статуса:"
claude mcp list | grep "$SERVER_NAME"

echo ""
echo "✨ Готово! Сервер перезагружен и готов к использованию."
